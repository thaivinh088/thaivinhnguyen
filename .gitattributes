# Giữ LF trong repo cho mọi file text. Không có dòng này thì Git tự đổi sang
# CRLF trên Windows, và hash nội dung mà build.mjs dùng để cache-bust sẽ khác
# nhau giữa các máy.
* text=auto eol=lf

*.png  binary
*.jpg  binary
*.webp binary
*.ico  binary
*.pdf  binary
*.woff2 binary
